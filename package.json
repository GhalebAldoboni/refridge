{
  "name": "feedforward",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run start --prefix=frontend && npm run start --prefix=backend/nodejs-server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.2",
    "openai": "^4.47.2"
  }
}
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend/nodejs-server && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run frontend\"",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
